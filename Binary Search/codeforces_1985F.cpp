
// Problem link ===>> https://codeforces.com/problemset/problem/1985/F
// submission link ===>>

#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll h, n;
        cin >>h>> n;

        vector<ll> a(n);
        for (ll i = 0; i < n; i++){
            cin >> a[i];
        }

        vector<ll> c(n);
        for (ll i = 0; i < n; i++){
            cin >> c[i];
        }

        
    }
    return 0;
}