#include <bits/stdc++.h>
using namespace std;

int maximumStability(int m, int n)
{
    
}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n, m;
        cin >> n >> m;

        int s = maximumStability(n, m);
        cout << s << endl;
    }
    return 0;
}